import 'package:flutter/material.dart';

/// A utility class for defining custom colors commonly used in the Flutter app.
class ColorUtility {
  /// Represents the magenta color with a specific opacity.
  static Color magenta = const Color.fromRGBO(107, 38, 75, 1.0).withOpacity(0.8);

  /// Represents the blue-grey color provided by the Flutter framework.
  /// This color is taken directly from the Flutter framework's `Colors` class.
  static Color blueGrey = Colors.blueGrey;

  /// Represents the white color provided by the Flutter framework.
  ///
  /// This color is taken directly from the Flutter framework's `Colors` class.
  static Color white = Colors.white;
}
