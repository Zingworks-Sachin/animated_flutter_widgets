enum Direction {
  left,
  right,
  up,
  down
}
enum PathDirection {
  forward,
  reverse,
}
enum CollectionAnimationType {
  listColored,
  waterFall,
  fadeOut,
  bounce,
  leftScale,
  rightScale,
  scaleOut
}
