enum Direction {
  left,
  right,
  up,
  down
}
enum PathDirection {
  forward,
  reverse,
}
enum CollectionAnimationType {
  listColored,
  stepAnimation,
  fadeOut,
  slideAndBounce,
  leftScaleAnimation,
  rightScaleAnimation,
  scaleOut
}
