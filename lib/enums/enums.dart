enum Direction {
  left,
  right,
  up,
  down
}
enum PathDirection {
  forward,
  reverse,
}
enum CollectionAnimationType {
  colorChange,
  stepAnimation,
  fadeOut,
  slideAndBounce,
  leftScaleAnimation,
  rightScaleAnimation,
  scaleLoad
}
